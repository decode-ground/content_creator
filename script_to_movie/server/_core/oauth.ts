import type { Express } from "express";

/**
 * OAuth routes placeholder
 *
 * The Manus OAuth integration has been removed.
 * This file is kept for potential future OAuth provider integrations.
 */
export function registerOAuthRoutes(app: Express) {
  // No OAuth routes configured
  // Future integrations (Google, GitHub, etc.) can be added here
}
